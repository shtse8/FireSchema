{
  "name": "@shtse8/fireschema",
  "version": "0.1.3",
  "description": "",
  "main": "dist/cli.js",
  "bin": {
    "fireschema": "dist/cli.js"
  },
  "scripts": {
    "test": "jest",
    "clean": "bunx rimraf dist",
    "build:workspaces": "bun run --filter '@shtse8/fireschema-runtime' build",
    "build:generated-ts": "bun run --filter '@my-project/firestore-models-ts' build",
    "build:root": "bun run clean && tsc",
    "build": "bun run build:workspaces && bun run build:generated-ts && bun run build:root",
    "prepublishOnly": "bun run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "generated/ts"
  ],
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.13.16",
    "bun": "^1.2.8",
    "firebase": "^11.6.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.1"
  },
  "dependencies": {
    "@types/ejs": "^3.1.5",
    "ajv": "^8.17.1",
    "commander": "^13.1.0",
    "ejs": "^3.1.10"
  }
}
