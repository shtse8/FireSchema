{
  "name": "fireschema",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cli.js",
  "bin": {
    "fireschema": "dist/cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "Remove-Item -Recurse -Force dist; npm run build --workspaces --if-present; tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.13.16",
    "firebase": "^11.6.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@types/ejs": "^3.1.5",
    "ajv": "^8.17.1",
    "commander": "^13.1.0",
    "ejs": "^3.1.10"
  }
}
