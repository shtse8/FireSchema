{
  "name": "fireschema",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cli.js",
  "bin": {
    "fireschema": "dist/cli.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "npx rimraf dist",
    "build": "npm run clean && npm run build --workspace=@fireschema/ts-runtime && bun build ./src/cli.ts --outdir ./dist --format cjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.13.16",
    "bun": "^1.2.8",
    "firebase": "^11.6.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@types/ejs": "^3.1.5",
    "ajv": "^8.17.1",
    "commander": "^13.1.0",
    "ejs": "^3.1.10"
  }
}
