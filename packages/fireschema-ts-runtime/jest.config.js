/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // Optional: Add any other Jest configurations here
  // For example, specify test file patterns:
  // testMatch: ['**/__tests__/**/*.test.ts?(x)'],
};